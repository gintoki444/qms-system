import { Outlet } from 'react-router-dom';

// ==============================|| MINIMAL LAYOUT ||============================== //

console.log('MinimalLayout');
const MinimalLayout = () => (
  <>
    <Outlet />
  </>
);

export default MinimalLayout;
