// import { useState,useEffect } from 'react';

export const createQueues = async ({ reserve_id, total_quantity, brand_code }) => {
  const result = [reserve_id, total_quantity, brand_code];
  console.log(reserve_id)
  return result;
};
